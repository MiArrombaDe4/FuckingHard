# Project Instructions

- Use plain HTML/CSS/JavaScript modules for this starter base.
- Keep code modular under `src/`.
- Store profile mock data in JSON under `src/data/`.
- Add a short architecture explanation in `README.md`.
